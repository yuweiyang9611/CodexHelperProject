# 六项修复验证记录

日期：2026-09-12。基于主线 `19f7c1c`，应用版本保持 0.6.0-beta.2；不发布新版本。

## 修复与证据

| 项目 | 变更及回归 |
| --- | --- |
| 旧备份恢复 | 固定 schema 2 清单分别验证提交和回滚；源日志删除后 SQLite 留存仍为 100 Token。schema 1/3 延续既有恢复及回滚测试。schema 2 的检查点不触碰账本。 |
| 状态条 | 用户移动、程序移动、锁定、待保存退出、备份文件清理、找回后重启、屏幕约束；真实 Electron E2E 断言找回和重启均保持 automatic。 |
| 验证入口 | 临时 dotnet/npm/node 替身在依赖安装、测试、构建、打包阶段返回 17；入口非零退出，调用记录停在失败命令，错误保留阶段和退出码。CI 接入同一测试。 |
| 完整校验 | Codex/Claude 等长改写且恢复 mtime；启动、59/60 分钟边界、读取失败、取消、运行时隔离、工作区切换、索引重建及损坏缓存。 |
| 历史缓存 | 未变化来源不再反序列化或写入账本；Codex 切换工作区保持缓存；账本外部替换后重新读取；保留 fork 去重与日期计价测试。 |
| 诊断文档 | 冲突与读取失败分别计数；新增最近完整校验时间和待校验状态，更新六套诊断截图。补充首次主线已通过的安装生命周期证据。 |

## 工具链与回归

.NET SDK 10.0.400、Node 22.23.2，沿用两套仓库 lockfile。C# 497 项、Web 单元 181 项、Electron 98 项通过；完整解决方案包含 WPF 严格构建通过。真实 Electron 测试使用独立临时目录，覆盖重启、历史留存、备份恢复、状态条及 Sidecar/renderer 故障恢复。

Web 视觉、键盘及 axe 无障碍矩阵 40 项通过，92 项按原有项目规则跳过。新增校验信息的六套诊断页基线已检查并更新，其余基线未改动。

最终 Electron 打包及打包目录 smoke 通过：`CODEXU_ELECTRON_SMOKE_OK`。版本一致性、发布链检查和仓库隐私检查通过；40 项依赖许可清单无变更。

本机脚本故障注入使用安装的 PowerShell 7；退出码判断不依赖 7.3/7.4 的实验或新增特性。未在当前主机安装额外 PowerShell 版本。

## 固定 30,000 行 Claude 样本

下表前后两次运行使用相同规模的合成 transcript，均为索引阶段；运行时存在其他本机负载，耗时不是性能保证。

| 未变化索引阶段 | 优化前记录 | 优化后记录 |
| --- | ---: | ---: |
| transcript 内容读取字节 | 0 | 0 |
| 解析完整行 | 0 | 0 |
| 分配字节 | 162,701,912 | 11,944 |
| 耗时 ms | 280.59 | 2.09 |

优化后完整刷新另测得 11.41 ms、分配 89,472 字节；测试进程工作集采样为 298,479,616 字节。完整刷新包含索引、账本合并和投影，工作集包含测试宿主且不是操作峰值，不能与上表的索引阶段混为同一指标。

CI 断言未到校验周期的刷新为零 transcript 内容读取、零解析行、零历史来源反序列化及零来源写入，不使用耗时阈值。追加仍完整读取旧前缀进行校验，只解析新增完整行；本次追加样本解析 1 行。

## 保留的限制

特殊等长改写可能延迟到首次启动读取或一小时到期后的下一次刷新才发现；关闭应用或停止刷新期间不会校验。校验失败保持待校验状态，并保留有效历史。

Windows 10 22H2、Win+D、Explorer 重启、混合 DPI 和多屏移除仍需专用虚拟机验收。本次不重启开发者 Explorer，不修改真实日志，不在开发者账户执行安装/卸载。
